# LiGaMD README illustration prompt, v2

This revision preserves the three-replica method while replacing the miniature
machine metaphor with a sparse computational-biophysics graphical abstract.

## Final style-transfer prompt

```text
Use the previous illustration only as a semantic reference. Preserve canonical
bound complex -> exactly three separate replicas -> geometry and persistence
endpoint -> real-frame sampling -> per-replica dynamics plus static features ->
optional frozen experimental-pKoff model, including an out-of-scope branch.

Use a pure white background, flat orthographic layout, accurate protein ribbon
and ball-and-stick ligand inserts, thin charcoal rules, strict alignment, and
ample whitespace. Keep about 80% grayscale. Use muted teal, ochre, and
terracotta to identify the three replicas, dark green for valid state, and
muted red for out of scope.

Show one large canonical bound HSP90-ligand reference; exactly three separate
horizontal trajectory lanes; three independently applied endpoint panels;
three real-frame contact sheets; three separate dynamic feature rows; one
static feature matrix; aggregation only after replica-level features; and a
small feature receipt connected by a dashed line to a detachable optional
frozen-model block and an assay-derived output with an uncertainty bar. A
failed episode must branch to out of scope before aggregation.

Do not generate fake numeric results. Experimental labels must not select
endpoints, frames, or features. Do not imply physical koff from simulation
time. Remove brass, vintage instruments, film-reel toys, freezer boxes, analog
gauges, paper texture, desks, cartoon arrows, robots, AI brains, neon, purple,
blue-black, DNA, stopwatch imagery, clay rendering, logos, and watermarks.
```
