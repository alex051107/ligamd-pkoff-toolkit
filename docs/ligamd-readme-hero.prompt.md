# LiGaMD README illustration prompt

Generated with the built-in Image 2 workflow as a full-width 16:9 GitHub README
hero. The illustration introduces the scientific objects; the SVG retains the
exact method and scope boundary.

## Final prompt

```text
Create a publication-quality editorial scientific illustration that makes the
LiGaMD pKoff Toolkit understandable immediately: three separate
molecular-dynamics replicas are analyzed label-blind, real trajectory frames
become static and dynamic features, and only then may an optional frozen model
predict an assay-derived experimental pKoff.

Build one continuous panoramic computational-biophysics workbench. Establish a
canonical bound HSP90 protein-ligand complex at the left. Start exactly three
separate trajectory lanes from that bound pose, each with its own continuous
conformational trace. Pass every lane independently through the same
geometry-and-persistence endpoint instrument and a contact-sheet reel of real
saved conformations. Divert an incomplete episode into a restrained
out-of-scope tray. Feed valid outputs into a compact static descriptor panel
and three separate dynamic time-series panels; combine corresponding dynamic
values only after replica-level calculation. Finish with a sealed feature
receipt entering a physically detachable frozen-model module and a restrained
assay-result gauge with uncertainty and applicability markings.

Use accurate protein ribbons and ball-and-stick ligand representations,
translucent trajectory overlays, real-frame contact sheets, etched glass,
uncoated paper, and brushed metal. Use warm ivory, graphite, muted sage,
restrained teal, ochre, and terracotta for the three replicas, plus small brass
details. Do not use purple, blue-black, neon, an AI brain, robot, chatbot,
cyberpunk UI, DNA, decorative glassware, a stopwatch, generic boxes and arrows,
readable prose, a fake numeric result, logos, or a watermark.

Keep the replicas separate until feature aggregation. Experimental labels must
not choose endpoints, frames, or features. Do not imply a physical
dissociation rate calculated from simulation time.
```
